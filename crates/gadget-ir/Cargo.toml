[package]
name = "gadget-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "DEPRECATED: re-export shim over circuit-ir (use circuit-ir + frontend-toml)"

[dependencies]
circuit-ir = { path = "../circuit-ir" }
toml = "0.8"
