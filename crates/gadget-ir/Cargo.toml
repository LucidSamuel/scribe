[package]
name = "gadget-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal IR for ZK gadget constraint systems"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
